# Wiki Lint Report — 2026-09-03

由 agent 执行的 lint（结构检查 + 语义检查）。对应 AGENTS.md 的 Lint Workflow。

## 结构检查（确定性，零 LLM）

| 检查项 | 结果 |
|---|---|
| 孤立页（无入链） | **NONE** ✅（修过：synthesis 页曾被 overview 孤立，已补链） |
| 断链（指向不存在页） | **NONE** ✅（修过：4 处把 wikilink 双中括号当正文写，被解析断链，已改为纯文本） |
| 稀疏页（出链 < 2） | **NONE** ✅ |
| 缺实体页（≥3 处提及却无专属页） | **NONE** ✅（Karpathy / RAG / Obsidian 都有页） |

> 两个真实问题在 demo 中被发现并修复——这正展示了 LLM Wiki "健康检查" 的价值：
> 结构 bug（孤页 / 断链）会被自动抓出来，而不是埋在库里。

## 语义检查（agent 判断）

### 矛盾（Contradictions）
- **[[LLMWiki]] 的"复利 vs 负债"**：[[karpathy-llm-wiki访谈]] 与
  [[为什么放弃LLM知识库]] 对同一机制（自动改写 / 补链 / 健康检查）给出相反价值
  判断。已在两个 source 页的 `## Contradictions` 与 `LLMWiki` 概念页中显式记录。
  这是**有意保留的实质矛盾**（两源真实对立），不是错误。

### 过时内容（Stale）
- 无。三源均为首次 ingest，无被后续来源推翻的旧结论。

### 数据缺口（Data Gaps）—— 建议补的源
- **"查询/ingest 比"的量化证据**：裁决复利 vs 负债的关键变量，当前 wiki 无材料。
  建议：找一份带真实使用统计的 LLM Wiki 案例（多少源 / 多少查询）。
- **半自动折中方案是否存在**：只自动建链、但正文不自动改写，能否规避"漂移"？
  反方主要攻击自动改写，若此折中存在则能显著削弱其论点。缺材料。
- **[[RAG]] 的正面证据**：目前 RAG 页只有"被 LLM Wiki 方批"的一面，没有 RAG
  适用场景（超大语料、实时查询）的正面材料，综合页因此略偏向 LLM Wiki。

### 需要加深的概念
- [[HealthCheck]]：目前只有"做什么"，缺"成本/收益"维度（反方把它归入漂移来源，
  应补一页讨论定期重写的风险）。
- [[KnowledgeRot]]：对策只指向 LLM Wiki，缺"非 AI 手段"（如时间戳标注、定期人工
  复审）的对照。

## 建议的下一步源
1. 一份 LLM Wiki 真实使用统计（带 query 数）→ 裁决主线矛盾
2. 半自动（只建链不改写）知识库的讨论 → 削弱反方
3. RAG 适用场景的正面材料 → 平衡 RAG 页
